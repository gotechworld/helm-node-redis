apiVersion: v1
kind: Service
metadata:
  name: {{ include "hapi-cms-api.fullname" . }}
  labels:
    {{- include "hapi-cms-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hapi-cms-api.selectorLabels" . | nindent 4 }}
